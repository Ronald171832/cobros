<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rldevelopers.cobros.tresenrayas">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tre_en_raya"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Vistas.Tres_En_Raya"
            android:label="Tres en Raya">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Vistas.Login"
            android:label="Login">
        </activity>
        <activity
            android:name=".Vistas.Menu_Principal"
            android:label="Menú Principal" />
        <activity
            android:name=".Trabajador.Trabajador"
            android:label="Trabajadores" />
        <activity
            android:name=".Trabajador.Login_Trabajador"
            android:label="Login Trabajador" />
        <activity
            android:name=".Trabajador.Menu_Trabajador"
            android:label="Menú Trabajador" />
        <activity
            android:name=".Cliente.Cliente"
            android:label="Clientes" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Cliente.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Cliente.AgregarCliente"
            android:label="@string/title_activity_agregar_cliente" />
        <activity
            android:name=".Cuenta.Cuenta"
            android:label="Cuentas" />
    </application>

</manifest>